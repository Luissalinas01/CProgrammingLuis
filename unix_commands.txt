mkdir <directory_name> (make directory). creates a folder.
cd <directry_name> (change directory). Changes the current directory to the specified one.
code <file_name>. Create a file and open it in a text editor
ls (list). shows the path of the current directory 
pwd (print working directory). show the path of the current directory.
clear - clears the terminal window 
gcc <file_name (c source code)> -compile the program.
./<executable_file> - runs the program.